const { Lead, Manager, Property, Worker } = require('../models');

const createManagers = (res, req) => {

}

const createProperties = (res, req) => {
    
}

const assignManager = (res, req) => {
    
}

const login = (res, req) => {
    
}

module.exports = {
  createManagers,
  createProperties,
  assignManager,
  login
}